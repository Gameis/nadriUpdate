create table newzipcode (
zipcode   varchar2(7),
sido   varchar2(20),
sigungu   varchar2(30),
yubmyundong   varchar2(20), -- 읍면동
ri   varchar2(20),
roadname   varchar2(100),
buildingname   varchar2(100));

집코드파일을 오라클 developer에 넣는법
데이터 임포트 하고  파일 찾아보기에서 csv파일을 지정한다
주소가 db에 기록된다

